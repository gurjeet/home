[general]
accounts = edb
ignore-readonly = yes

[Account edb]
localrepository = gurjeetedblocal
remoterepository = gurjeetedb

[Repository gurjeetedblocal]
type = Maildir
localfolders = ~/gurjeet.singh_enterprisedb.com_archive

[Repository gurjeetedb]
type = Gmail
remotehost = imap.gmail.com
folderfilter = lambda folder: folder == 'INBOX' or folder == '[Gmail]/Sent Mail' or folder == '.ToMe'
#folderfilter = lambda folder: folder == '[Gmail]/Chats'
maxconnections = 3
readonly = true
remoteuser=gurjeet.singh@enterprisedb.com
# remotepass =

