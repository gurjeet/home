{
  # When you wish to install a Nix package that is not supported on the current
  # system, remove the DONT_ prefix from the `allowUnsupportedSystem` property.
  DONT_allowUnsupportedSystem = true;
}

